import { ConsolePlaceholder } from "@/components/console-page";
export default function VerificationsPage() { return <ConsolePlaceholder title="Verification History" description="Review prior TrustID verification activity when the workflow is enabled." message="Verification workflows will appear here." />; }
