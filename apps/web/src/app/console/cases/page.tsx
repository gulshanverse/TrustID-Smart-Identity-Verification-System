import { ConsolePlaceholder } from "@/components/console-page";
export default function CasesPage() { return <ConsolePlaceholder title="Cases" description="Organize review and investigation work when case management is enabled." message="Case management is not enabled in this phase." />; }
