import type { Metadata } from "next";
import { LoginPlaceholder } from "@/components/marketing";

export const metadata: Metadata = { title: "TrustID Secure Console — Sign In", description: "Secure console entry placeholder for the future authorized TrustID verification workflow." };
export default function LoginPage() { return <LoginPlaceholder />; }
