import type { Metadata } from "next";
export const metadata: Metadata = { title: "TrustID Console — Authorized Operations", description: "Protected TrustID operational console for authorized personnel." };
export default function ConsoleLayout({ children }: { children: React.ReactNode }) { return children; }
